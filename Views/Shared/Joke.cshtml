@model XCoreAssignment.DTOs.JokeDTO

<div class="container d-flex justify-content-center align-items-center" style="height: 100vh;">
    <div class="card p-4 shadow-lg rounded-3 animate__animated animate__fadeInUp" style="max-width: 500px;">
        <h1 class="fw-bold text-center mb-4" style="font-family: 'Montserrat', sans-serif; font-size: 2rem;">Success! Now to the joke...</h1>
        <div class="d-grid gap-3 fs-5 text-center">
            <p>Setup: @(string.IsNullOrWhiteSpace(Model.Setup) ? "/" : Model.Setup)</p>
            <p>Delivery: @(string.IsNullOrWhiteSpace(Model.Delivery) ? "/" : Model.Delivery)</p>
        </div>
    </div>
</div>
